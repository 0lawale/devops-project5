name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Validate Monitoring Stack
    runs-on: ubuntu-latest
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Docker Compose
        run: |
          DOCKER_CONFIG=${DOCKER_CONFIG:-$HOME/.docker}
          mkdir -p $DOCKER_CONFIG/cli-plugins
          curl -SL https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64 \
            -o $DOCKER_CONFIG/cli-plugins/docker-compose
          chmod +x $DOCKER_CONFIG/cli-plugins/docker-compose
          docker compose version

      - name: Validate Prometheus config
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/prometheus:/etc/prometheus \
            --entrypoint promtool \
            prom/prometheus:v2.50.0 \
            check config /etc/prometheus/prometheus.yml

      - name: Validate Alertmanager config
        run: |
          sudo apt-get install -y gettext-base
          envsubst < alertmanager/alertmanager.yml > /tmp/alertmanager-resolved.yml
          docker run --rm \
            -v /tmp/alertmanager-resolved.yml:/etc/alertmanager/alertmanager.yml \
            --entrypoint amtool \
            prom/alertmanager:v0.27.0 \
            check-config /etc/alertmanager/alertmanager.yml

      - name: Start monitoring stack
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: docker compose up -d

      - name: Debug - check running containers
        run: |
          docker compose ps
          docker compose logs alertmanager | tail -20

      - name: Wait for services to be healthy
        run: |
          echo "Waiting for services to start..."
          sleep 60

      - name: Check Prometheus is up
        run: |
          for i in {1..10}; do
            if curl --fail http://localhost:9090/-/healthy; then
              echo "Prometheus is healthy"
              break
            fi
            echo "Attempt $i failed, retrying in 5s..."
            sleep 5
          done

      - name: Check Grafana is up
        run: |
          for i in {1..10}; do
            if curl --fail http://localhost:3000/api/health; then
              echo "Grafana is healthy"
              break
            fi
            echo "Attempt $i failed, retrying in 5s..."
            sleep 5
          done

      - name: Check Alertmanager is up
        run: |
          for i in {1..10}; do
            if curl --fail http://localhost:9093/-/healthy; then
              echo "Alertmanager is healthy"
              break
            fi
            echo "Attempt $i failed, retrying in 5s..."
            sleep 5
          done

      - name: Check all targets are being scraped
        run: |
          curl http://localhost:9090/api/v1/targets | \
          python3 -c "
          import sys, json
          data = json.load(sys.stdin)
          targets = data['data']['activeTargets']
          down = [t for t in targets if t['health'] == 'down']
          if down:
              print('DOWN targets:', [t['labels']['job'] for t in down])
              sys.exit(1)
          print('All', len(targets), 'targets are up')
          "

      - name: Tear down stack
        if: always()
        run: docker compose down